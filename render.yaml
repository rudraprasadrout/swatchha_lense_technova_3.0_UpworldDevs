services:
  - type: web
    name: swachhlens-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend.utils.app:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: MISTRAL_API_KEY
        sync: false
